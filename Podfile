# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WeatherTron' do
  use_frameworks!

  # Pods for WeatherTron
    pod "PromiseKit", "~> 4.4"
    pod "PromiseKit/CoreLocation"
    pod "JGProgressHUD"

  target 'WeatherTronTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
